# Area Testing Rules

Added from inside the TEAM repo's `area/` subtree, then pushed back to the
standalone area repository via `git subtree push`.

- Require unit tests for new code.
- Keep test coverage above 80%.
